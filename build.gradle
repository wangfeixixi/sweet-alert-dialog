buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
    }
}

ext {
    compileSdkVersion = 21
    buildToolsVersion = "19.1.0"
}

allprojects {
    repositories {
        mavenCentral()
        google()//新增的
    }
}
